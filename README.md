# DB Connector + REPL + Docker

This project contains:
- A connectors library (MongoDB + PostgreSQL)
- An IDBConnector interface
- Full unit tests
- A REPL console application
- A Dockerfile to run the REPL app

## Running the REPL

```bash
dotnet run --project src/DBConsoleClient
